---
- hosts: all
  become: true
  gather_facts: true
  roles:
    - common
    - willshersystems.sshd
    - NTP
    - singleplatform-eng.users
    
- name: Install NGINX on proxy
  hosts: webservers
  become: true
  roles:
    - geerlingguy.nginx
