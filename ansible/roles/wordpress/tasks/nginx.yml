- name: add nginx yum rpm package
  copy:
    src: http://nginx.org/packages/centos/7/noarch/RPMS/nginx-release-centos-7-0.el7.ngx.noarch.rpm
    dest: /tmp/nginx.rpm

- name: install nginx from downloaded yum rpm package
  yum:
    name: /tmp/nginx.rpm
    state: present
    notify: restart_nginx